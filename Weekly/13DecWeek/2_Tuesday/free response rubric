appropriately initializes scanner class 

asks for user input in each round

prints approriate message within each round

uses loop to generate correct number of guesses

uses .equals method for boolean comparison



Reordering arrays: These are a big more complicated. They require:  Create a new temp array that is the same size as the original
Copy elements from the original array to the new array in the order that you want
When finished, copy the temp array back over on top of the original array to replace it.

Create a new temp array that is the same size as the original
Copy elements from the original array to the new array in the order that you want
When finished, copy the temp array back over on top of the original array to replace it.

